import React from "react";
import { StyledSpinnerContainer } from "./styles/StyledSpinner";

const NotFound = () => (
  <StyledSpinnerContainer>Whoops... nothing found here...</StyledSpinnerContainer>
);

export default NotFound;
