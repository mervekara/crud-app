import React from "react";
import { StyledSpinnerContainer, StyledSpinner } from "../styles/StyledSpinner";

const Spinner = () => (
  <StyledSpinnerContainer>
    <StyledSpinner />
  </StyledSpinnerContainer>
);

export default Spinner;
